let idade = 100 //Declaraçao da variável

  if(idade >= 0 && idade <= 12){ //Caso true >= 0 e <=12
   console.log (`Criança`); //Apresentar Mensagem `Criança`
   }
    else if (idade >= 13 && idade <= 17){ //Caso true >=13 e <=17
      console.log(`Adolescente`); //Apresentar Mensagem `Adolescente`
    }
   if (idade >= 18 && idade <= 59){ //Caso true >=18 e <= 59
    console.log(`Adulto`); //Apresentar Mensag5em `Adulto`
    }
    else if (idade >= 60 && idade<= 120){ //Caso true >=60
     console.log(`Idoso`); // Apresentar Mensagem `Idoso`
    }
   if(idade > 120);{ //Caso o usuário digite uma idade acima de 120 anos      console.log(`Idade inválida`);
     console.log (`Digite uma idade válida`)
   }
     console.log(`Fim do código.`)
    
      
    